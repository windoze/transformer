t1(fv as float, os as string, appBundle as string, osVersion as string)
| project bucketId = bucket(fv, 1, 2, 4, 8, 16, 32, 64, 128, 256, 512), appBundleCrossOS = os + "_" + appBundle, majorOSVersion = split(osVersion, ".")[0]
;
